from appcontroller import AppController

if __name__ == '__main__':
    # Crea una instancia de la clase `AppController`.
    ctrl = AppController()
    # Llama al método `loop` de la instancia `ctrl` para iniciar el bucle interactivo.
    ctrl.loop()
